services:
  - type: web
    name: dj-set-scraper-api
    env: python
    plan: starter
    buildCommand: |
      pip install -r requirements.txt
      python -m playwright install chromium
    startCommand: uvicorn main:app --host 0.0.0.0 --port 10000